services:
  sigal:
    build: .

    ports:
      - "8080:8080"

    environment:
      TITLE: "TITLE"
      THUMB_SIZE: 350
      PORT: 8080

    volumes:
      - ./photos:/gallery:ro
      - ./site:/output